# Models

Placeholder for future AI model assets and inference adapters.

Suggested split:

- `gesture/`: hand-gesture recognition models using micro:bit / IMU sensor data.
- `pitch/`: pitch or audio transcription models if the project moves beyond the current backend transcription pipeline.
- `shared/`: preprocessing, feature extraction, and model metadata used by both local tests and Raspberry Pi deployment.
